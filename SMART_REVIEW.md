# 스마트 복습

앱의 시작 화면과 하단 메뉴에서 매일 최대 5문항을 복습합니다. 다른 탭에서도 오늘 완료 여부를 안내합니다. 자료가 적으면 사용 가능한 문항 모두를 풉니다. 영어·한국어가 함께 있는 영작, AI 첨삭이 저장된 다듬은 표현, 주요 표현에서 중복을 제거하고 오래 안 본 자료·최근 오답·발화 횟수가 적은 자료를 우선 선정합니다. 2,000자를 넘는 자료는 문장으로 나눠 저장해야 합니다.

- 영어 → 한국어 해석 / 한국어 → 영어 작문을 번갈아 출제합니다.
- AI는 문장의 의미를 채점합니다. 다른 표현도 정답이 될 수 있습니다. AI 장애·오판에 대비해 답안 입력 후 참고 답안과 비교하는 직접 채점도 제공합니다.
- 모든 답안을 **저장**해야 그날 완료됩니다. 정답률은 연속 기록에 영향을 주지 않습니다.
- 날짜는 모든 기기에서 Asia/Seoul(한국 시간)로 고정합니다. 오늘이 끝나기 전에는 어제까지의 연속 기록을 유지하고, 미완료 날짜가 지나면 끊깁니다. 과거 날짜 입력, 기록 변경, 삭제, 연속 학습 복구 UI는 없습니다.
- 성장 단계: 새싹(시작), 어린나무(7일), 큰나무(14일), 숲(30일), 울창한 숲(45일), 큰숲(60일), 오래된 원시림(90일), 열대우림(120일), 아마존(150일), 거대한 아마존(180일), 생명의 낙원(210일), 초록 대륙(240일), 푸른 행성(300일), 생명의 지구(365일). 현재 연속 기록으로 성장하며 최고·누적 기록은 남습니다.

## 저장 및 동기화

Google 로그인과 인터넷 연결이 필요합니다. `/users/{uid}/smartReviews/{YYYY-MM-DD}`에 하루당 문서 하나를 저장하므로 사용자 프로필 한 문서의 1 MiB 한도를 누적 기록이 소진하지 않습니다. 질문·원문·참고 답안은 시작 시 고정되어 원본 학습 자료를 편집해도 과거 복습은 바뀌지 않습니다. 시작 및 답안 저장은 Firestore 트랜잭션이며 먼저 저장된 문항·답안을 유지합니다. 저장 실패를 성공으로 표시하지 않습니다. 실시간 구독을 통해 PC·모바일에서 이어서 풀 수 있습니다.

최근 28일 달력, 날짜별 전체 문제·내 답안·정오답·채점 방식·피드백, JSON 내보내기를 제공합니다. 기존 계정 백업과 별개로 복습 화면의 **전체 기록 내려받기**에서 내보냅니다. 내보내기는 보관용이며 연속 기록을 되살리는 가져오기 기능은 없습니다.

## 배포 순서

1. PR의 빌드, 타입 검사, 회귀 테스트, Firestore 에뮬레이터 검사를 통과시킵니다.
2. Firebase 프로젝트 `daily-english-studio-d182a`의 Firestore 규칙에 이 저장소 `firestore.rules`의 `/users/{userId}/smartReviews/{date}` 규칙을 게시합니다. 기존 운영 규칙과 차이가 있으면 기존 규칙을 유지하고 해당 블록만 추가합니다. 소유자만 읽고 쓸 수 있으며 완료 기록 변경·삭제·지난 날짜 쓰기를 금지합니다.
3. main에 병합하여 기존 GitHub Pages 워크플로로 배포합니다. GitHub Pages 배포는 Firebase 규칙을 자동으로 게시하지 않습니다.
4. 같은 Google 계정으로 PC에서 시작 → 모바일에 같은 문제 표시 → 일부 답안 기록 → PC 반영 → 완료 후 양쪽의 날짜·성장 단계 확인. 기존 사용자의 실제 복습 기록을 테스트 데이터로 채우지 않습니다.

## 검증

`npm run lint`, `npm test`, `npm run build`.

Firestore 통합 검사(Java 21 필요):

```
npx firebase-tools emulators:exec --only firestore --project demo-smart-review "node --test tests/firestore-rules.integration.mjs"
```

AI 채점은 Firebase AI Logic/App Check의 기존 설정 및 가용성에 의존합니다. AI 오류 시 직접 채점으로 오늘 복습을 마칠 수 있습니다. 학습용 연속 기록이며 클라이언트 사용자가 의도적으로 요청을 위조하는 부정행위 방지 시스템은 아닙니다.

성장 여정은 두 잎 새싹(3일), 작은 숲(21일)을 포함한 16단계입니다. 3일부터 나비·꿀벌·새·토끼 등 동물이 한 마리씩 늘어 365일에 15마리가 됩니다. 풍경과 동물은 현재 연속 기록에 따라 성장하며, 연속 기록이 끊기면 새싹부터 시작합니다. 스마트 복습 탭은 5개 하단 메뉴의 세 번째(정가운데)에 배치합니다.

## 발음 확인 및 녹음 저장

AI 튜터의 발음 확인 팝업은 기기의 영어 음성으로 입력한 영어를 읽습니다. 0.75/1/1.25배속과 정지를 지원합니다. 입력은 저장하거나 AI에 전송하지 않으며 닫으면 지웁니다. 실제 음색과 지원 여부는 기기의 음성 엔진에 따라 다릅니다.

녹음 실패의 코드상 원인은 녹음당 고정 400,000 Base64 문자 제한(약 300KB), 브라우저 기본 녹음 비트레이트, 누적 Base64를 localStorage에 저장할 때의 용량 초과였습니다. 파일 업로드의 FileReader 비동기 오류가 바깥 try/catch를 빠져나가는 문제도 수정했습니다.

새 녹음은 모노 32kbps를 요청합니다. 기기가 다른 품질로 녹음하거나 저장 가능한 용량을 초과하면 기기 안에서 MP3로 압축합니다. 원본 음성·한국어 대본 등 문서 전체의 UTF-8 크기를 고려하여 Firestore 1 MiB보다 작은 900,000바이트 예산을 적용합니다. 너무 긴 음성은 무제한 저장할 수 없으며, 압축해도 넘으면 안내하고 미리듣기·파일 내려받기를 유지합니다. 성공 전에 저장 내용을 확정하지 않습니다.

localStorage가 차면 음성 바이너리를 IndexedDB에 보관하고 작은 메타데이터만 localStorage에 씁니다. 실제 바이너리 쓰기 실패는 성공으로 숨기지 않습니다. 새로고침과 백업 내보내기는 IndexedDB 음성을 다시 포함하며, 원본 오디오를 찾지 못하면 백업을 중단합니다. 실제 마이크 기기별 검증은 별도로 필요하지만 20/60/120초 합성 음성의 MP3 인코딩 및 저장 예산, 용량 초과 후 IndexedDB 저장·복원·실패 처리를 회귀 테스트로 검증합니다.
